sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    return Controller.extend("com.co.stratesys.zpscopiarproyectos.controller.App", {
        onInit: function () {
            // El SplitApp se gestiona automáticamente
            // por el router (sap.m.routing.Router) definido en el manifest.json
        }
    });
});